# 'protocol' and 'root_path' are required config options
# Protocol is one of `fsspec.available_protocols()` values
# Additional parameters wil be used as kwargs to fsspec.filesystem(protocol, **kwargs) call
protocol=file
root_path=/tmp/torchx_jobs
