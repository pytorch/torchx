"""
description: Echos a message
arguments:
  - name: --msg
    help: Message to echo
    default: "hello world"
    type: str
  - name: --image
    help: Image to run
    default: /tmp
    type: str
"""

import torchx.specs as specs


echo = specs.Role(
    name="echo",
    entrypoint="/bin/echo",
    args=[args.msg],
    container=specs.Container(image=args.image),
    num_replicas=1,
)

app = specs.Application(name="echo", roles=[echo])

export(app)
